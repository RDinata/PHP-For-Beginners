<?php

$business = [
    'name' => 'Metal Gears Co.',
    'cost' => 10000,
    'categories' => ['industrial', 'widgets', 'sprockets']
];

function register($user)
{
    // create the user record in the db.
    // log them in.
    // send them a welcome email.
    // redirect to their new dashboard.
}

require "index.view.php";
